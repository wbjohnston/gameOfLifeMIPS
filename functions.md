LIST OF ALL FUNCTIONS IN PSEUDO-C

void memset(dest, num_bytes, set_val)

void print_banner(n)

int input_size()

int input_generation()

bool is_occupied(board, x, y)

void advance_gen(destination, source)

int find_neighbors(board, x, y)

void reset_board(source, destination)

void return()

void print_board(board)

int input_coords(dest[])

void populate(coords[])
